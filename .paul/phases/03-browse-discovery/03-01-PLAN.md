---
phase: 03-browse-discovery
plan: 01
type: execute
autonomous: true
---

<objective>
Goal: Implement search (command-palette overlay + results page) and proper browse pages
  for /series and /movies with client-side genre filtering.

Purpose: Users can find anime via the header search shortcut, explore results on a
  dedicated search page, and filter the series/movies catalogue by genre — all with the
  same dark, cinematic, anime-artwork aesthetic as the homepage.

Output:
  - SearchOverlay component (command-palette style, triggered from header search icon)
  - /search results page
  - Rewritten /series and /movies browse pages with genre filter tabs
  - Shared mock catalog data layer (replaces hardcoded page content)
  - Header updated with search trigger icon
</objective>

<context>
@apps/web/components/home/HomeHeader.tsx
@apps/web/components/home/MediaCard.tsx
@apps/web/components/home/AnimatedSection.tsx
@apps/web/components/home/TiltCard.tsx
@apps/web/components/home/SpotlightSection.tsx
@apps/web/components/home/types.ts
@apps/web/components/discovery/EditorialHero.tsx
@apps/web/components/discovery/PosterStrip.tsx
@apps/web/app/[locale]/(main)/series/page.tsx
@apps/web/app/[locale]/(main)/movies/page.tsx
@apps/web/constants/routes.ts
@apps/web/app/globals.css
</context>

<acceptance_criteria>
Given the user clicks the search icon in the header
  When the overlay opens
  Then it shows an input, mock instant results, and is closeable via Escape

Given the user types in the search overlay
  When results are shown
  Then pressing Enter or clicking a result navigates to /en/watch/[slug]

Given the user visits /en/series
  When the page loads
  Then they see a cinematic hero, a genre filter bar, and a grid/rail of anime cards

Given the user clicks a genre chip on /en/series
  When a genre is active
  Then the card grid filters to matching items (client-side, no navigation)

Given the user visits /en/movies
  When the page loads
  Then they see the same cinematic browse layout (hero, genre filter, grid)

Given the user visits /en/search?q=demon
  When the page loads
  Then search results are pre-filtered by the query and the input is pre-filled
</acceptance_criteria>

<tasks>

<task type="auto" id="03-01-T01">
Create mock catalog data layer

File: apps/web/components/discovery/catalog.data.ts

Export:
- catalogItems: ReadonlyArray<CatalogItem> — 16 items mixing series + movies
  Fields: { id, slug, title, type: "series"|"movie", genre: string[], rating, year,
            episodes?: number, badge?: string, imageSrc: string, description: string }
- genres: ReadonlyArray<{ id: string; label: string; emoji: string }> — 10 genres
  (all, action, romance, psychological, comedy, fantasy, thriller, isekai, classic, school)

Use placeholder images: /images/placeholder.png, /images/placeholder1.png, /images/card.png, /images/promo.png
(rotate across items — 4 available images, 16 items)

This is a TypeScript-only file, no JSX.
</task>

<task type="auto" id="03-01-T02">
Create CatalogCard component

File: apps/web/components/discovery/CatalogCard.tsx

Props: { item: CatalogItem; priority?: boolean }

Layout (poster card, same visual language as home/MediaCard.tsx):
- aspect-poster (2/3) rounded-[1.5rem] border border-white/12
- Image fill, opacity-60 group-hover:opacity-80
- Gradient overlay from black bottom
- Top-left: year badge (tiny pill)
- Bottom: title (font-bold line-clamp-2) + genre tags row (2 max)
- Rating pill bottom-right: ★ X.X in primary color
- Shimmer sweep on hover (same .shimmer-sweep pattern from globals.css)
- Below card: title line-clamp-1 + type badge ("Series" | "Movie")
- Wrap in Link href={routes.watch(item.slug)}

No motion (parent handles AnimatedSection).
</task>

<task type="auto" id="03-01-T03">
Create GenreFilterBar component

File: apps/web/components/discovery/GenreFilterBar.tsx
"use client"

Props: { genres: ReadonlyArray<{ id: string; label: string; emoji: string }>;
         active: string; onSelect: (id: string) => void }

Layout:
- Horizontal scrollable row, no scrollbar (same pattern as FrictionKiller)
- Each chip: pill button, same active/inactive styles as home/GenreChips.tsx
- "All" chip always first (id: "all")
</task>

<task type="auto" id="03-01-T04">
Create BrowsePage component (shared layout for /series and /movies)

File: apps/web/components/discovery/BrowsePage.tsx
"use client" (needs useState for active genre)

Props:
  type: "series" | "movie"
  hero: { badge: string; headline: string; subtext: string }

Behaviour:
- Imports catalogItems + genres from catalog.data.ts
- Filters by type (series/movie), then by active genre ("all" = no genre filter)
- useState for activeGenre, default "all"

Layout (dark, cinematic — matches homepage aesthetic):
1. Browse hero strip (NOT the old EditorialHero):
   - Dark card: bg-[linear-gradient(135deg,rgba(11,28,3,0.96),rgba(5,5,5,0.98))]
     rounded-[2rem] border border-white/8 p-8 md:p-10
   - Green glow blob top-left (same radial-gradient pattern as PromoBanner)
   - Left col: small badge (green uppercase tracking), big headline (text-4xl font-black),
     subtext (text-foreground/62), stat pills row (X titles / Y genres / "Free episode")
   - No right column (clean, left-weighted)

2. GenreFilterBar — mounted below hero

3. Results grid:
   - AnimatedSection wrapping the grid
   - CSS grid: grid-cols-2 sm:grid-cols-3 md:grid-cols-4 xl:grid-cols-5 gap-4
   - Map filtered items → CatalogCard
   - If 0 results: centered empty state ("Nothing in this genre yet — try another mood")

4. No pagination (all mock data fits in one view)
</task>

<task type="auto" id="03-01-T05">
Rewrite /series page

File: apps/web/app/[locale]/(main)/series/page.tsx

Replace entire file with:
- Import BrowsePage
- Pass hero data specific to series
- No other content (BrowsePage handles everything)
</task>

<task type="auto" id="03-01-T06">
Rewrite /movies page

File: apps/web/app/[locale]/(main)/movies/page.tsx

Same pattern as series but type="movie" and movie-specific hero copy.
</task>

<task type="auto" id="03-01-T07">
Create SearchOverlay component

File: apps/web/components/discovery/SearchOverlay.tsx
"use client"

Props: { open: boolean; onClose: () => void }

Behaviour:
- Keyboard: Escape → onClose, Arrow ↑↓ navigate results, Enter → navigate to result
- Input autofocused when open (useEffect + ref)
- Filters catalogItems by title/genre match (case-insensitive) as user types
- Empty query → show 6 "Featured" items (first 6 of catalog)
- Max 8 results shown at once

Layout (command-palette style, Aceternity-influenced):
- Fixed overlay: inset-0 z-[500] bg-black/75 backdrop-blur-sm
- Centered modal: max-w-xl w-full mx-4 mt-24 rounded-[1.8rem]
  bg-[linear-gradient(180deg,rgba(12,12,12,0.98),rgba(8,8,8,0.99))]
  border border-white/10 shadow-[0_32px_120px_rgba(0,0,0,0.6)]
- Top: search input row (magnifier icon + input + Esc badge)
  input: bg-transparent text-foreground placeholder:text-white/30 focus:outline-none
- Divider border-b border-white/8
- Results list: each row is a Link with poster thumbnail (40px), title, type badge, genre tag
  Highlighted row: bg-white/[0.05]
- Footer: "16 titles available" or "{n} results for "{query}""

Use framer-motion AnimatePresence for mount/unmount (opacity + y: -8 → 0).
</task>

<task type="auto" id="03-01-T08">
Add search trigger to HomeHeader

File: apps/web/components/home/HomeHeader.tsx

Changes:
1. Add useState for searchOpen
2. Import SearchOverlay (dynamic import with ssr: false)
3. Add search icon button in right actions (before "Sign in"):
   - SVG magnifier icon, 36px tap target, text-white/50 hover:text-foreground
   - onClick → setSearchOpen(true)
4. Render <SearchOverlay open={searchOpen} onClose={() => setSearchOpen(false)} />
5. Keyboard shortcut: useEffect adds window keydown listener for Cmd+K / Ctrl+K → open
</task>

<task type="auto" id="03-01-T09">
Create /search results page

File: apps/web/app/[locale]/(main)/search/page.tsx

Server component — reads searchParams.q

Layout:
- Same max-w-screen-2xl px-4 md:px-8 container as other pages
- Page header: "Results for "{q}"" or "Search" if no query
- Renders BrowsePage-style grid (not the full BrowsePage — just the filtered grid)
  OR import a thin SearchResults client component that handles the query filter

Simplest correct approach:
- Create apps/web/components/discovery/SearchResults.tsx ("use client")
  Props: { initialQuery: string }
  Same filter logic as SearchOverlay, but in full-page grid layout (same CatalogCard grid)
  Includes a search input at top (to refine query without navigating)
- /search/page.tsx reads params and passes to SearchResults
</task>

<task type="auto" id="03-01-T10">
Add /search to routes + update header nav

File: apps/web/constants/routes.ts
- Add: search: (q?: string) => withLocale(q ? `/search?q=${encodeURIComponent(q)}` : "/search")

File: apps/web/components/home/HomeHeader.tsx
- Update SearchOverlay to navigate to routes.search(query) on Enter in the overlay
  (already handled in SearchOverlay via Link — just ensure hrefs use routes.watch)
</task>

<task type="auto" id="03-01-T11">
Run typecheck and fix any errors

Command: cd /Users/ahmedelfarra/Code/work/animekey-v2 && npm run typecheck 2>&1 | head -60

Fix all TypeScript errors before marking done.
</task>

<task type="auto" id="03-01-T12">
Commit and push to new branch

Branch: feature/ANI-browse-discovery-03-01
Commit message: "feat(browse): search overlay, genre filter, browse pages — Phase 3 03-01"

Commands:
  git checkout -b feature/ANI-browse-discovery-03-01
  git add apps/web/components/discovery/ apps/web/app apps/web/constants/routes.ts apps/web/components/home/HomeHeader.tsx
  git commit -m "feat(browse): search overlay, genre filter, browse pages — Phase 3 03-01"
  git push -u origin feature/ANI-browse-discovery-03-01
</task>

</tasks>

<boundaries>
DO NOT CHANGE:
- globals.css (already has all needed keyframes)
- apps/web/app/globals.css
- Any auth components
- Homepage components (AnimatedSection, TiltCard, SpotlightSection may be imported but not modified)
- The existing EditorialHero / PosterStrip (they can be deleted from series/movies pages but left as files)

SCOPE LIMITS:
- No real API integration — all data is static mock in catalog.data.ts
- No pagination
- No "continue watching" or personalization (Phase 7)
- No watchlist (Phase 7)
- Show detail page is Phase 3 Plan 03-02 — /watch/[id] stays untouched
</boundaries>

<verification>
- [ ] npm run typecheck exits 0
- [ ] Header shows search icon; clicking it opens overlay
- [ ] Cmd+K opens overlay from any page
- [ ] Escape closes overlay
- [ ] Typing filters results instantly
- [ ] /en/series loads with hero + genre filter + card grid
- [ ] Clicking a genre chip on /en/series filters the grid (no page navigation)
- [ ] /en/movies loads similarly
- [ ] /en/search?q=demon shows filtered results with pre-filled input
</verification>
