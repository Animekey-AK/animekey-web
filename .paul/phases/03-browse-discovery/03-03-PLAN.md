---
phase: 03-browse-discovery
plan: 03
type: execute
autonomous: true
---

<objective>
Goal: Build the /new-and-hot page with "Trending Now" and "New Releases" sections, fix the nav link, and close Phase 3.
Purpose: Complete the Browse & Discovery phase. Users need a curated page for trending and recently-added content.
Output: /new-and-hot page, routes constant, nav fix, typecheck clean, visual verified, committed, PR raised.
</objective>

<context>
@apps/web/components/discovery/catalog.data.ts
@apps/web/components/discovery/CatalogCard.tsx
@apps/web/components/discovery/BrowsePage.tsx
@apps/web/constants/routes.ts
@apps/web/components/home/HomeHeader.tsx
@apps/web/app/[locale]/(main)/series/page.tsx
</context>

<acceptance_criteria>
Given I navigate to /en/new-and-hot
When the page loads
Then I see a "Trending Now" section and a "New Releases" section each populated with CatalogCards

Given the nav header is visible
When I look at "New & Hot" link
Then it points to /en/new-and-hot (not /en/series)

Given TypeScript strict mode
When I run typecheck
Then 0 errors

Given the page is visually tested
When I screenshot at 1440px full-page
Then both sections render with correct cards and layout
</acceptance_criteria>

<tasks>
<task type="auto" id="1">
Add `newAndHot` to routes.ts

In apps/web/constants/routes.ts, add:
  newAndHot: withLocale("/new-and-hot"),
after the `series` line.
</task>

<task type="auto" id="2">
Fix "New & Hot" nav link in HomeHeader

In apps/web/components/home/HomeHeader.tsx, change:
  { label: "New & Hot", href: routes.series }
to:
  { label: "New & Hot", href: routes.newAndHot }
</task>

<task type="auto" id="3">
Create NewAndHotPage component

File: apps/web/components/discovery/NewAndHotPage.tsx
"use client" component with two sections:

Section 1 — "Trending Now":
- Shows with badge containing "TRENDING", "TOP 10", "EDITOR'S PICK", or rating >= 8.8
- Sort by rating descending
- Max 12 items
- Title: "🔥 Trending Now" + "What fans are watching this week" subtext

Section 2 — "New Releases":
- Shows with badge "NEW", "MASTERPIECE", "LATEST", or year >= 2024
- Sort by year descending then rating
- Deduplicate against trending list (no item in both)
- Max 12 items
- Title: "🆕 New Releases" + "Just added to AnimeKey" subtext

Layout:
- Dark hero strip at top (matches BrowsePage hero strip pattern): cinematic dark bg with green glow accents, badge "NEW & HOT", large headline "What's hot\nright now."
- Page body: max-w-screen-2xl mx-auto px-4 md:px-8 py-10
- Each section: <h2> heading + count badge pill + AnimatedSection-wrapped 2/3/4/5-col grid of CatalogCard
- Gap between sections: space-y-16

Import: catalogItems, genres from catalog.data
Import: CatalogCard from ./CatalogCard
Import: AnimatedSection from @/components/home/AnimatedSection
</task>

<task type="auto" id="4">
Create /new-and-hot page route

File: apps/web/app/[locale]/(main)/new-and-hot/page.tsx

```tsx
import { NewAndHotPage } from "@/components/discovery/NewAndHotPage";

export default function NewAndHotRoute() {
  return <NewAndHotPage />;
}
```
</task>

<task type="auto" id="5">
Run typecheck

cd apps/web && npx tsc --noEmit
Must return 0 errors.
</task>

<task type="checkpoint:human-verify" id="6">
Visual test — /new-and-hot page

Navigate to http://localhost:3000/en/new-and-hot
Take full-page screenshot at 1440×900
Verify:
- Hero strip renders with "NEW & HOT" badge and headline
- "Trending Now" section has multiple CatalogCards
- "New Releases" section has multiple CatalogCards
- Nav "New & Hot" link is active/highlighted

Also verify the "New & Hot" nav link works from homepage.
</task>

<task type="auto" id="7">
Commit Phase 3 final

Stage and commit:
- apps/web/constants/routes.ts
- apps/web/components/home/HomeHeader.tsx
- apps/web/components/discovery/NewAndHotPage.tsx
- apps/web/app/[locale]/(main)/new-and-hot/page.tsx
- .paul/STATE.md
- .paul/phases/03-browse-discovery/03-02-SUMMARY.md
- .paul/phases/03-browse-discovery/03-03-PLAN.md

Commit message:
feat(discovery): new-and-hot page with trending and new releases sections

Closes Phase 3 Browse & Discovery.
</task>

<task type="auto" id="8">
Raise PR for Phase 3

Push feature/ANI-browse-discovery-03-01 and create PR:
- Base: develop
- Title: "feat(discovery): browse pages, search, show detail, and new & hot — Phase 3 [ANI-browse]"
- Body: summary of all 3 plans in Phase 3
</task>
</tasks>

<boundaries>
DO NOT CHANGE: existing catalog.data.ts, show.data.ts, or any Phase 2 components
SCOPE LIMIT: Do not build a real recommendations engine — use badge/year/rating filtering from existing catalog data
DO NOT MODIFY: proxy.ts, auth.config.ts, lib/auth.ts
</boundaries>

<verification>
- [ ] /en/new-and-hot loads without error (200)
- [ ] "New & Hot" nav link active on /en/new-and-hot
- [ ] Trending section: 8+ cards
- [ ] New Releases section: 4+ cards
- [ ] TypeScript: 0 errors
- [ ] PR raised
</verification>
