---
phase: 03-browse-discovery
plan: 02
type: execute
autonomous: true
---

<objective>
Goal: Rebuild /watch/[id] into a real show detail page with episode list.

Purpose: When a user clicks any card from the homepage, search, or browse pages,
  they land on a cinematic title detail page — not a stub. They see the show's
  synopsis, metadata, episode list, and "More like this" recommendations.

Output:
  - show.data.ts — per-show episode data keyed by slug
  - ShowDetailHero — cinematic full-bleed backdrop hero with metadata + CTAs
  - EpisodeCard — thumbnail + number + title + duration card
  - EpisodeList — scrollable episode rail with "Season X" heading
  - ShowDetail — master layout component
  - /watch/[id] page.tsx — rewrite using ShowDetail
</objective>

<acceptance_criteria>
Given the user visits /en/watch/demon-slayer
  When the page loads
  Then they see a cinematic hero with Demon Slayer's title, rating, genres, description

Given the user looks at the episode list
  When the list renders
  Then they see episode cards with number, title, and "Ep 1 is free" badge

Given the user looks at "More like this"
  When it renders
  Then they see CatalogCards for related shows from the same genre

Given the user visits /en/watch/unknown-slug
  When the page loads
  Then a graceful fallback shows the slug-derived title with placeholder data

Given typecheck runs
  Then it exits 0 with no errors
</acceptance_criteria>

<tasks>

<task type="auto" id="03-02-T01">
Create show episode data

File: apps/web/components/discovery/show.data.ts

Export:
interface Episode {
  number: number;
  title: string;
  duration: string; // "24 min"
  description: string;
  isFree: boolean;
  imageSrc: string;
}

interface ShowDetail {
  slug: string;
  title: string;
  tagline: string;
  description: string;
  rating: number;
  year: number;
  studio: string;
  genres: string[];
  episodes: ReadonlyArray<Episode>;
  imageSrc: string;
  backdropSrc: string; // same as imageSrc for now
  type: "series" | "movie";
}

showDatabase: Record<string, ShowDetail> — entries for all 16 catalog slugs.
Series: 4-6 mock episodes each.
Movies: 1 "episode" (the film itself), isFree: true for ep 1.

Use /images/placeholder.png, /images/placeholder1.png, /images/card.png, /images/promo.png for both imageSrc and backdropSrc.
Rotate images across slugs.

Export getShow(slug: string): ShowDetail — returns the DB entry or a generated fallback.
</task>

<task type="auto" id="03-02-T02">
Create EpisodeCard component

File: apps/web/components/discovery/EpisodeCard.tsx

Props: { episode: Episode; showSlug: string; isActive?: boolean }

Layout (landscape card, 280px wide):
- Outer: <Link href={routes.watch(showSlug)} prefetch={false}>
- Image area: aspect-video (16/9), rounded-[1.2rem], relative overflow-hidden
  - Image fill, opacity-70 group-hover:opacity-90 transition
  - Dark gradient overlay bottom half
  - Episode number badge top-left: "E{number}" in tiny pill
  - "FREE" badge top-right if isFree (green primary pill)
  - Play button overlay center (opacity-0 group-hover:opacity-100)
- Below image:
  - Episode title (text-sm font-semibold line-clamp-1)
  - Duration (text-xs text-foreground/45)
  - Description (text-xs text-foreground/55 line-clamp-2 mt-1)
</task>

<task type="auto" id="03-02-T03">
Create EpisodeList component

File: apps/web/components/discovery/EpisodeList.tsx

Props: { episodes: ReadonlyArray<Episode>; showSlug: string; totalEpisodes?: number }

Layout:
- Section header: "Episodes" (font-bold text-xl) + episode count pill
- Horizontally scrollable row (same pattern as ContentRail: overflow-x-auto, hidden scrollbar)
- Maps episodes to EpisodeCard
- If totalEpisodes > episodes.length: trailing "..." card showing remaining count
  (same width as EpisodeCard, dashed border, centered text: "+{remaining} more episodes")
</task>

<task type="auto" id="03-02-T04">
Create ShowDetailHero component

File: apps/web/components/discovery/ShowDetailHero.tsx

Props: { show: ShowDetail }

Layout (cinematic hero, no framer-motion — server renderable):
- Outer: relative overflow-hidden, min-h-[60vh]
- Backdrop layer: absolute inset-0, Image fill blur-3xl opacity-20 scale-110
  (gives atmospheric dark background from the show's image)
- Dark gradient on top of backdrop: absolute inset-0,
  bg-[linear-gradient(to_right,rgba(5,5,5,0.97)_0%,rgba(5,5,5,0.85)_50%,rgba(5,5,5,0.3)_100%)]
- Content grid: grid md:grid-cols-[minmax(0,1fr)_auto] gap-8 items-end p-6 md:p-10 relative z-[1]

Left column (content):
  - Genres row: each genre in a tiny pill (border border-white/12 bg-white/[0.04])
  - Title: text-5xl md:text-7xl font-black tracking-tight text-foreground (leading tight)
  - Metadata row: rating ★, year, studio, type badge — all separated by · in text-foreground/55
  - Description: max-w-2xl text-[15px] leading-7 text-foreground/70 line-clamp-3
  - CTA row:
    Primary: "▶ Watch Episode 1 Free" → routes.watch(show.slug)
      className: same green pill as homepage hero (bg-primary text-primary-foreground shadow-[0_16px_48px_rgba(113,199,4,0.28)] px-6 py-3.5 rounded-full font-bold)
    Secondary: "Add to Watchlist" → # (placeholder)
      className: border border-white/15 bg-white/[0.04] px-6 py-3.5 rounded-full text-sm font-medium text-foreground/80

Right column (poster):
  - Hidden on mobile, visible md+
  - w-48 shrink-0 aspect-[2/3] rounded-[1.8rem] overflow-hidden border border-white/15
    shadow-[0_32px_80px_rgba(0,0,0,0.6)]
  - Image fill object-cover opacity-90
</task>

<task type="auto" id="03-02-T05">
Create ShowDetail layout component

File: apps/web/components/discovery/ShowDetail.tsx

Props: { show: ShowDetail; relatedShows: ReadonlyArray<CatalogItem> }

Layout:
1. <ShowDetailHero show={show} />
2. Container div: mx-auto max-w-screen-2xl px-4 md:px-8 py-10 space-y-12
3. <EpisodeList episodes={show.episodes} showSlug={show.slug} totalEpisodes={show.episodes.length} />
4. "More like this" section:
   - Heading: "More like this" (text-xl font-bold)
   - Horizontally scrollable row of CatalogCard (shrink-0) — same pattern as ContentRail
   - Use AnimatedSection wrapper
5. Paywall note strip (if show has more than 1 episode and not all free):
   - Between episode list and more-like-this
   - Thin strip: border-y border-white/8 bg-white/[0.02] py-4 px-4
   - Text: "🔒 Episodes 2+ require a subscription — 7-day free trial, no card needed."
   - Link "Start free trial →" → routes.plans
</task>

<task type="auto" id="03-02-T06">
Rewrite /watch/[id] page

File: apps/web/app/[locale]/(main)/watch/[id]/page.tsx

Server component — reads params.id.
- Import getShow from show.data.ts
- Import catalogItems from catalog.data.ts
- Call getShow(id) to get show data
- Compute relatedShows: catalogItems filtered by matching any genre with show.genres, excluding self, max 8
- Render <ShowDetail show={show} relatedShows={relatedShows} />
- No container padding at page level — ShowDetailHero is full-width, ShowDetail handles its own container
</task>

<task type="auto" id="03-02-T07">
Run typecheck and fix errors

Command: npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -60
Fix all errors.
</task>

<task type="auto" id="03-02-T08">
Visual test with Playwright/Chrome DevTools

Start dev server, navigate to http://localhost:3000/en/watch/demon-slayer.
Take screenshot. Check:
- Cinematic hero with backdrop, title, metadata, CTAs
- Episode list visible and scrollable
- "More like this" rail
- Navigate to /en/watch/your-name (movie) — should show 1 episode card
</task>

<task type="auto" id="03-02-T09">
Commit and push

Branch: feature/ANI-browse-discovery-03-01 (same branch — all Phase 3 browse work)
Commit: "feat(watch): show detail page + episode list — Phase 3 03-02"
Push.
</task>

</tasks>

<boundaries>
DO NOT CHANGE:
- Any auth components
- Homepage components
- globals.css
- GenreFilterBar, BrowsePage, SearchOverlay (built in 03-01)

SCOPE LIMITS:
- No actual video playback (Phase 4)
- No real API — all data from show.data.ts
- No user-specific data (watchlist, continue watching — Phase 7)
- DRM + HLS = Phase 4
</boundaries>

<verification>
- [ ] /en/watch/demon-slayer shows cinematic hero + episode list + related
- [ ] /en/watch/spirited-away (movie) shows correctly (1 episode, "The Film")
- [ ] TypeScript exits 0
- [ ] All catalog slugs resolve (no crashes on unknown slug)
</verification>
